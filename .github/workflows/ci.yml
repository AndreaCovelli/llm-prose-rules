name: CI

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install uv
        run: python3 -m pip install --upgrade uv

      - name: Install Vale
        run: |
          curl -L https://github.com/errata-ai/vale/releases/download/v3.15.1/vale_3.15.1_Linux_64-bit.tar.gz -o vale.tar.gz
          tar -xzf vale.tar.gz vale
          sudo install vale /usr/local/bin/vale

      - name: Install dependencies
        run: |
          uv sync --locked --dev
          sudo apt-get update
          sudo apt-get install -y shellcheck

      - name: Run test harness
        run: uv run ./scripts/test.sh

      - name: Check rule fixtures
        run: uv run python scripts/check-rule-fixtures.py

      - name: Check rule metadata
        run: uv run python scripts/check-metadata.py

      - name: Run shellcheck
        run: shellcheck scripts/*.sh

      - name: Run pre-commit
        run: uv run pre-commit run --all-files

      - name: Run Vale
        run: vale --config=.vale.ini .
