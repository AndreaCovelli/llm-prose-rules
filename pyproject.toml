[project]
name = "llm-prose-rules"
version = "0.1.2"
description = "Vale rules for cleaning up LLM-assisted prose and commit messages."
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = [
  "pre-commit>=4.6.0",
  "pyyaml>=6.0.3",
]

[tool.uv]
package = false
